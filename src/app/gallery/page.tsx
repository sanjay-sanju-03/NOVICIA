import { PublicPage } from "@/components/public/site-shell";
export default function GalleryPage() { return <PublicPage eyebrow="THE ARCHIVE" title="The story is about to unfold."><div className="grid grid-cols-2 gap-4 md:grid-cols-4">{Array.from({ length: 8 }, (_, index) => <div key={index} className={`min-h-40 rounded-2xl border border-white/10 bg-gradient-to-br ${index % 3 === 0 ? "from-cyan-400/30 to-violet-600/10" : index % 3 === 1 ? "from-violet-500/30 to-slate-950" : "from-amber-300/20 to-rose-600/10"} p-4`}><p className="font-mono text-sm text-white/60">NOVICIA / {String(index + 1).padStart(2, "0")}</p></div>)}</div><p className="mt-8 max-w-xl text-slate-300">The post-event gallery will preserve the opening, the energy, the late-night ideas, and the dawn.</p></PublicPage>; }
